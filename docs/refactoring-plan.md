# TODO-08：职责拆分实施计划

规划日期：2026-09-12。起点提交：`335d3bf`，工作区干净。

目标是让接口、日志扫描、缓存、刷新状态和界面绘制可以分别修改。按以下顺序实施并验收；价格规则、统计口径、缓存 v4 编码、CLI/MCP 字段和菜单展示沿用现有行为。

## 阶段一：官方数据访问

- [x] 将官方请求及认证一致性检查移入 `OfficialUsageClient`，注入账户来源、App Server 调用和 HTTP 请求。
- [x] 将响应转换、JSON 支持、Codex 可执行文件发现和传输分别归位。现有 `CodexAccountContext` 继续负责账户身份。
- [x] Backend 保留调用方使用的公开方法；账户及响应模型回归直接验证新组件。

验收：账户来源、重置券回退、字段缺失与响应转换测试通过，应用编译通过。

## 阶段二：本机统计与外部入口

- [x] 将本机扫描器从 Backend 移出，缓存文档与锁定、读取、原子写入归入独立存储组件。
- [x] 日志发现、解析辅助及来源路径归入本机统计模块；扫描事务仍在同一进程锁及文件锁范围内。
- [x] 将 CLI、MCP 与插件安装拆为独立文件，复用进程发现及 JSON 支持。

验收：扫描、重启、重建、取消及旧缓存回归通过；隔离 CLI/MCP 检查确认输出兼容。

## 阶段三：刷新控制与 AppKit

- [x] 在 Core 中建立不依赖 AppKit 的刷新控制器，复用 `RefreshCoordinator` 的代次、合并和退避规则。
- [x] 注入数据客户端、时钟、任务执行器及额度历史处理；状态变更、提醒通过回调交给应用。
- [x] 将菜单卡片、偏好及应用入口分开。AppDelegate 负责菜单、通知权限及系统睡眠/网络事件的转发。
- [x] 补充控制器层的失败恢复、账户切换、超时迟到、睡眠/唤醒、请求合并和重建传递验证。

验收：上述场景可用假客户端及可控执行器测试，无真实账户、联网或墙钟等待；现有 AppKit 场景继续通过。

## 阶段四：整体交付

- [x] 更新架构说明和 UI 验证的源文件收集方式。
- [x] 运行 `make verify` 和 256 MiB 的 `make benchmark`，与 TODO-07 的结果对比。
- [x] 备份当前状态后安装，核对签名、官方和本机状态，以及账户和周观察基线。
- [x] 更新 TODO 完成记录，提交仓库改动。

## 迁移约束与风险

- 尽量保持同一 Swift target，新增实现类型默认 internal，避免为文件拆分扩大公开接口。
- 缓存字段、编码方式及版本保持兼容；锁的持有范围和取消时回滚必须覆盖整个扫描事务。
- 刷新控制器是唯一的数据状态持有者，界面读取只读状态；旧账户及过期任务不得提交数据或发出提醒。
- 后台调用跨队列只传递 Sendable 值，保留官方/本机各一条工作队列。
- 各阶段先编译并运行相关测试；最后统一执行完整验证。性能对比使用相同合成数据及 release 构建，耗时波动结合 RSS 和缓存大小判断。

## 实施记录

- 阶段一：官方数据客户端、响应转换及进程传输独立；28 项账户/响应/刷新状态检查通过。
- 阶段二：扫描器、缓存与外部入口独立；78 项扫描及账户相关检查和隔离 CLI/MCP 集成通过。Backend 从 2,672 行缩为约 90 行，保持公开读取入口。
- 阶段三：控制器和唯一数据状态迁入 Core，AppKit 拆为应用接入、事件转发、偏好和四个卡片文件。11 项新增控制器检查通过，覆盖可控时钟下的失败恢复、账户/上下文切换、超时迟到、睡眠/唤醒、网络恢复、重建合并和退出。
- 实现依赖保持同一 Swift target；测试直接引用新的 internal 组件，没有为文件迁移保留旧 Backend 内部命名代理。

- 阶段四：`make verify` 的 162 项常规测试、CLI/MCP、7 种 AppKit 场景及独立应用资源检查全部通过；完整浅色、不可用深色渲染均已检查。四个卡片提取前后的绘制代码及缓存模型字段另行逐项比较一致。
- `make benchmark`（256 MiB）通过，tokens 和全量/增量金额一致。峰值 RSS 从约 31 MB 变为约 29 MB，缓存均为 3,870 字节；以下单次耗时没有明显退化，毫秒级差异会受运行环境影响。

| 操作 | TODO-07 基线 | 本次 |
| --- | --- | --- |
| 冷扫描 | 1.41 秒 | 1.51 秒 |
| 无变化增量 | 1.46 毫秒 | 1.61 毫秒 |
| 追加 | 129 毫秒 | 145 毫秒 |
| 重启复用缓存 | 1.78 毫秒 | 1.47 毫秒 |
| 完整重建 | 1.70 秒 | 1.51 秒 |

安装前备份了旧应用及 4 份状态文件。安装后签名校验通过，运行中的应用与构建产物一致；额度、余额、重置券和本机统计均成功，扫描完整，账户及周观察基线保持不变，已有 460 条样本保留。未自动切换真实登录、网络或系统睡眠；这些人工验收范围继续见刷新说明。
