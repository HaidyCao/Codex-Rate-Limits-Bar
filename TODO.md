# 项目优化 TODO

基于 2026-09-11 的项目审查整理。勾选项已完成，其余待推进；复现结果来自隔离样例，不代表真实账户已经发生相同错误。

每次选择一项推进：补充有意义的回归测试、实施修复、完成验收，再勾选对应任务并记录变更。统计修复优先于结构重构。

## 第二轮审查（2026-09-12）

按本轮建议先处理明细计价和官方数值校验，再处理副本去重。原 TODO-01～08 的完成记录保留；下列为复查发现的边界问题。

- [x] TODO-09（P1，2026-09-12）：保留缺失或矛盾明细的 token 总量，API/credits 标为未计价；累计差值保留不确定性，后续完整明细可恢复计价。界面隐藏不可靠的分项和命中率，周估值暂停使用受影响区间；旧缓存重算保留周观察。
- [x] TODO-10（P2，2026-09-12）：官方额度缺失比例时显示不可用；拒绝布尔值、分数、越界整数、非法计数、余额和日期，窗口时长限制防止秒数换算溢出，保留明确零值。
- [x] TODO-11（P1，2026-09-12）：按共同后续记录对齐互补累计历史，保留未汇合尾部；修复文件头空行识别，日/周分别去重，旧副本缓存自动重算并保留周观察。见[去重规则与验收记录](docs/copy-reconciliation.md)。
- [x] TODO-12（P2，2026-09-12）：提醒先保存待发送状态，通知中心接受后确认；提交和回调复查账户、开关及窗口，取消/失败可重试，独立请求编号隔离迟到回调。见[提醒发送与确认](docs/refresh.md#提醒发送与确认)。
- [x] TODO-13（P2，2026-09-12）：新增独立缓存保存状态，失败保留内存结果并在无新增日志时重试；保留取消校验、有效基线和同基线的并发观察。见[缓存保存与恢复](docs/cache-persistence.md)。
- [ ] TODO-14（P2，下一项）：app-server stdout 按字节缓冲至完整行后解码 UTF-8，并限制缓冲大小；覆盖多字节字符跨读取块导致的超时。
- [ ] TODO-15（P2）：插件安装先校验 marketplace，再暂存替换；CLI 安装失败时恢复旧插件和配置，避免损坏配置被覆盖。

**TODO-09～10 验收记录：** 首批 5 项回归在旧实现上产生 54 次断言失败，修复后通过；新增回归共 11 项，全部 173 项常规测试、CLI/MCP、10 种 AppKit 场景及独立应用资源检查通过。256 MiB 合成日志冷扫 1.66 秒、追加 0.18 秒、重建 2.03 秒，峰值 RSS 约 31 MB，缓存 3,870 字节，增量/重建结果一致。备份后安装新版，四项实际读取成功、扫描完整，账户和周观察基线不变，备份中的 653 条样本保留。规则说明见[计价完整性](docs/pricing.md)与[官方字段校验](docs/refresh.md)。

现场首次升级重扫了 308 个文件、约 4 GB 日志，耗时两分多钟；并发 CLI 曾等待缓存锁超时。升级完成后复验四项读取成功，全部文件使用新校验版本，原样本完整保留。

**TODO-11 验收记录：** 首批 6 项回归在旧实现上产生 64 次断言失败，修复后通过；本轮新增 16 项常规回归及 1 项副本性能测试，全部 189 项常规测试、CLI/MCP、10 种 AppKit 场景和独立应用资源检查通过。覆盖互补前缀、多组汇合/分叉、模型及 token 分项、fork 导入、跨日/跨 home、空行、旧缓存、冲突恢复和周观察重置。2 万个累计样本、8 个副本冷扫约 8.56 秒，重建约 9.52 秒，无变化约 0.0033 秒，缓存约 23 KB；复用日期解析器前分别约 39.07 秒和 38.82 秒。标准 256 MiB 样例冷扫约 1.87 秒、重建约 1.73 秒，两类样例的 tokens/API/credits 及增量/重建结果一致。备份后安装，新版与构建产物一致；5 个副本缓存完成升级，四项实际读取成功、扫描完整，无副本冲突，账户/周观察基线和原有 678 条样本保留。

**TODO-12 验收记录：** 睡眠取消后漏提醒、关闭开关后仍提交的两条回归在旧实现上产生 3 次断言失败；本轮新增 19 项回归，全部 208 项常规测试、CLI/MCP、10 种 AppKit 场景和独立应用资源检查通过。覆盖通知失败重试、同一提醒等待期间去重、45 秒无回调恢复、迟到确认、账户/服务端上下文变化、睡眠及退出、重置提醒跨重启重试与过期、确认写入失败后恢复、旧 v1 历史兼容。系统已接受的通知在保存确认时不会被睡眠撤销。备份后安装，新版与构建产物一致，四项实际读取成功、扫描完整，账户/周观察基线、原有 756 条周观察及三个额度历史文件的样本和去重记录保留。测试使用模拟通知回调；真实系统通知展示仍按人工步骤验收。

**TODO-13 验收记录：** 初始两项回归在旧实现上失败，确认写入失败不提示且无新增日志时不再保存。本轮新增 11 项回归，全部 219 项常规测试、CLI/MCP、12 种 AppKit 场景和独立应用资源检查通过；已检查两种新增保存失败场景的浅色/深色显示。覆盖首次及追加写入失败、原文件保留、无变化重试、取消/账户校验回滚、周样本合并及账户隔离、删除/损坏恢复、禁用缓存、旧快照兼容，以及同时读取失败的独立提示。缓存仍为 v4，无需重新定价或全量迁移。256 MiB 样例冷扫约 1.32 秒、重建约 1.41 秒、无变化约 0.0013 秒，缓存 3,895 字节；2 万样本/8 个副本冷扫约 6.19 秒、重建约 6.05 秒、无变化约 0.0017 秒，缓存 23,012 字节。两类样例的增量/重建结果一致，内存及缓存大小检查通过。备份后安装，四项实际读取成功、扫描完整、缓存状态为 `saved`，原有 772 条周观察、账户基线及额度历史保留。

## 优先级与执行顺序

| 编号 | 优先级 | 任务 | 执行安排 |
| --- | --- | --- | --- |
| TODO-01 | P1 | 统一账户来源，隔离预测和提醒历史 | 已完成（2026-09-11） |
| TODO-02 | P1 | 修复会话去重、来源归属和缓存失效 | 已完成（2026-09-11） |
| TODO-03 | P1 | 修复静默漏扫，展示统计完整性 | 已完成（2026-09-11） |
| TODO-04 | P2 | 改善周额度金额估值的可信度 | 已完成（2026-09-12） |
| TODO-05 | P2 | 价目表版本化与未知模型维护 | 已完成（2026-09-12） |
| TODO-06 | P2 | 展示数据时效，完善刷新恢复 | 已完成（2026-09-12） |
| TODO-07 | P2 | 完善回归测试和验证流程 | 已完成（2026-09-12） |
| TODO-08 | P3 | 拆分 Backend 和主界面职责 | 已完成（2026-09-12） |

## TODO-01：统一账户来源，隔离预测和提醒历史

**完成记录（2026-09-11）：** 已统一官方刷新及认证来源，预测/提醒按账户范围落盘，周金额观察带账户标识；菜单展示账户及来源。新增 10 项回归测试，全部 70 项测试通过，并验证两套真实登录及 CLI/MCP 隔离样例。首次升级时，无法确认归属的旧周观察重新建立；旧预测文件保留。无法确认身份的托管凭据模式保留官方值，并暂停账户历史和周金额学习。

**问题与证据：** 额度请求跟随 `CODEX_HOME`，重置次数查询默认读取 `~/.codex/auth.json`；预测和提醒共用 `quota-history.json`，没有账户维度。切换账户的样例中，混入旧样本后预测消耗速度由独立计算的约 0.51 个百分点/小时变成 15，并标记为高置信度；旧账户的提醒去重状态还会阻止新账户提醒。

- [x] 建立统一账户上下文，明确账户标识、Codex home、认证来源和额度类型。
- [x] 额度、官方 credits 余额、重置次数及本机周观察使用一致的账户来源；保留“今日全机合计”的明确口径。
- [x] 优先使用 `account/rateLimits/read` 的 `rateLimitResetCredits`，处理字段缺失、仅返回数量和旧版本兼容；需要回退时仍使用同一账户认证。
- [x] 按账户、目录和额度类型隔离预测样本、提醒状态及周金额观察；识别同一目录内重新登录其他账户的情况。
- [x] 限定重置时间漂移的历史迁移只能发生在同一账户上下文内。
- [x] 制定旧缓存迁移规则；无法确认归属的历史不直接并入新账户，同时保留可以确认归属的有效观察基线。
- [x] 在界面或详情中展示数据所属账户/来源，避免混淆全机统计与当前账户额度。

**验收：** 两个 home、同一 home 切换账户、不同额度类型互不串样本和提醒；同账户正常时间漂移仍能保留历史；重置次数和额度始终来自同一账户。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift)、[QuotaForecast.swift](Sources/CodexRateLimitsCore/QuotaForecast.swift)。接口依据：[官方 App Server 文档](https://learn.chatgpt.com/docs/app-server#6-rate-limits-chatgpt)。

## TODO-02：修复会话去重、来源归属和缓存失效

**完成记录（2026-09-11）：** 改为会话 ID 与用量事件指纹去重，保留副本来源及各自后续增量；文件身份、变更时间和已读前缀 SHA-256 校验负责缓存失效。新增菜单及 CLI 重建入口，有效账户和周观察基线保持不变。新增 12 项回归测试，全部 82 项测试通过，并通过 CLI 重建/MCP 一致性验证。真实日志的缓存副本验证约 4 GB 扫描，增量约 0.29 秒、完整重建约 28 秒，峰值内存占用约 40 MB；增量与随后重建结果一致，周观察未改变。不可核验的缺失副本保留原统计并提示错误，恢复后自动重算；无当前统计贡献的旧路径可安全清理。

**问题与证据：** 修复前主要按文件名去重、按长度和游标决定增量读取。已复现同大小替换后仍返回旧值、改名副本重复计数，以及跨 home 去重后丢失当前账户的周消耗。

- [x] 结合会话 ID、文件标识、内容指纹等信息识别会话和文件变化，避免仅依赖文件名。
- [x] 检测同大小覆写、原子替换、截断后重写和普通追加，按需重建受影响文件的统计。
- [x] 同一会话改名或复制后只统计一次；同名但不同会话的文件不能错误合并。
- [x] 明确同一会话存在分叉内容副本时的处理规则，不仅按文件大小选择副本。
- [x] 去重时保留全部来源归属，确保全机合计去重不会排除当前账户应计入的周消耗。
- [x] 保留既有 fork 导入事件去重、跨午夜基线和价格重算行为。
- [x] 提供“重建统计”入口，明确重建范围，保留仍有效的账户及周观察基线。

**验收样例：** 同大小日志从 100 tokens 改成 900 后，增量和全量扫描均返回 900；100 tokens 的相同会话改名副本合计仍为 100；跨 home 的相同日志全机只计一次，当前账户周消耗仍为应有的 `$0.40`。

相关代码：[LocalUsageScanner.swift](Sources/CodexRateLimitsCore/LocalUsageScanner.swift)、[LocalUsageCache.swift](Sources/CodexRateLimitsCore/LocalUsageCache.swift) 和 [UsageFileIdentity.swift](Sources/CodexRateLimitsCore/UsageFileIdentity.swift) 中的扫描、文件状态和去重逻辑。

## TODO-03：修复静默漏扫，展示统计完整性

**完成记录（2026-09-11）：** 移除依赖字节位置和格式的预筛选，完整解析有界 JSONL；单条上限提高到 8 MB，超过上限及未完成记录明确报告。新增扫描状态、目录/读取/缺失/解析/跳过诊断，分别展示定价覆盖与默认费率占比；缺少单次输入字段不会再当作已知零上下文。周金额按当前来源的完整性及计费条件暂停/恢复推算，保留有效观察基线。新增 13 项回归测试，全部 95 项测试通过；CLI/MCP 隔离集成检查纳入 `make verify-local-usage` 和 `make verify`。真实约 4 GB 日志的缓存副本验证：增量约 0.24 秒、重建约 41 秒，峰值内存约 47 MB，无读取/解析/跳过错误，增量与随后重建一致，周观察未变。

**问题与证据：** 修复前，一个文件无法读取时，结果仍可能是 `error=nil`、定价覆盖率 100%；合法 JSON 仅在冒号后增加空格，就会被预筛选跳过。缺失请求模式或上下文信息时采用默认费率，也不等于计费信息完整。

- [x] 让 usage 事件预筛选兼容合法 JSON 空白和字段顺序，同时验证大日志扫描性能。
- [x] 将目录遍历失败、文件读取失败、解析失败和被跳过的重要记录纳入结构化诊断。
- [x] 分别表达扫描完整性、模型定价覆盖率、默认计费条件所占比例，避免把单一覆盖率当作整体准确率。
- [x] 除现有模式缺失计数外，跟踪请求上下文缺失等会影响费率判断的信息。
- [x] 明确空目录、无法访问目录、全部记录解析失败和真实零用量的区别。
- [x] UI、CLI、MCP 使用一致的完整性状态；界面显示简短提示，详情提供失败数量和原因。
- [x] 数据不完整时，对相关估值降低置信度或暂停推算；文件恢复可读后自动补齐统计。

**验收：** 不可读文件或格式兼容问题不会悄悄产生“完整统计”；冒号后带空格的有效日志与紧凑 JSON 统计一致；有已知价格但信息缺失的数据可与完整计费记录区分。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift)、[Models.swift](Sources/CodexRateLimitsCore/Models.swift)、[AppText.swift](Sources/CodexRateLimitsCore/AppText.swift)。

## TODO-04：改善周额度金额估值的可信度

**完成记录（2026-09-12）：** 改为按官方读取时间与本机分钟消耗对齐，最近 24 小时内至少积累 3 个不重叠区间，每区间至少 30 分钟、变化 5 个百分点。展示向外取整的金额范围、样本置信度、有效区间与跨度；区间范围考虑本机假设的 2 分钟延迟及整数百分比误差，不作为统计置信区间。过期、断档、额度回退及异常比例会暂停或重新积累；未知 API 价格和影响 API 费率的条件缺失排除相应区间，仅影响 credits 的不确定性限制置信度为中。升级保留旧账户和周观察基线，新时间样本从升级后积累；重启、重建和改价保留有效时间样本。新增 19 项回归测试，全部 114 项测试及 CLI/MCP 集成检查通过。真实约 4 GB 日志的缓存副本验证：迁移约 0.09 秒、增量约 0.07 秒、重建约 40 秒，峰值内存占用约 63 MB，缓存约 491 KB，统计完整且周观察基线未变。

**问题与证据：** 修复前额度变化达到 2 个百分点、token 定价覆盖率达到 95% 即给出单点金额，已用 2 分钟样例复现过早输出 `$20`。整数百分比、更新延迟和短样本容易放大波动；token 覆盖比例也不等于金额覆盖比例。

- [x] 使用多个观察区间评估估值稳定性，制定最小观察时长和有效变化量规则。
- [x] 对齐本机消耗与官方额度采样时间，处理延迟更新和不连续样本。
- [x] 展示样本跨度、有效样本量、置信度或估值范围，避免不必要的小数精度。
- [x] 将未知模型、默认费率假设和扫描完整性纳入可信度判断，不单靠 95% token 覆盖率放行。
- [x] 识别账户切换、额度重置和明显异常区间；排除、降权或暂停估值，并给出原因。
- [x] 持续标明其他设备、云端活动不在本机分子中；周金额属于本机推算的 API 等价估值。

**验收：** 短样本和阶梯式百分比更新不会产生看似精确且高置信度的金额；稳定样本逐步收敛；不可靠数据会给出明确状态。

相关代码：[WeeklyQuotaEstimator.swift](Sources/CodexRateLimitsCore/WeeklyQuotaEstimator.swift)、[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift)、[AppText.swift](Sources/CodexRateLimitsCore/AppText.swift)。

## TODO-05：价目表版本化与未知模型维护

**完成记录（2026-09-12）：** 将 API 和 credits 的价格、别名、上下文及模式规则移至随应用打包的 JSON，分别记录版本、核验日期、来源与适用条件。新增完整自定义配置、导出及校验命令，每次扫描固定一份价目表；未知字段、重复键、无效价格/日期及错误别名会被拒绝，无效配置明确提示并回退内置版本。界面、CLI/MCP 展示版本、来源和未知模型/模式的原始名称、tokens、占比与原因，已知请求金额不再因同模型另一请求未定价而丢失。保留按当前价格重估的口径，记录指纹及最近一次版本变化；有效规则变化只重算受影响日志，纯版本/来源文字变化不会触发重算。修正 Cyber 未公布的长上下文 API 价格不应套用通用倍率。新增 14 项测试，全部 128 项测试及 CLI/MCP 集成检查通过；移走构建目录资源后，独立应用仍能读取内置价目表。真实约 4 GB 日志的缓存副本验证：迁移约 0.48 秒、增量约 0.06 秒、重建约 42 秒，峰值内存占用约 62 MB、缓存约 501 KB；迁移与重建的 tokens、API 金额和 credits 一致，账户基线及周采样历史未变。

**问题：** 修复前 API 价格、Codex credits 价格及模型别名写在 Swift 中，新增模型需要改代码和发版；价格调整后，用户也需要知道已有等价金额为什么变化。

- [x] 将价目表与模型别名整理为可校验的独立配置，保留内置默认版本。
- [x] 记录价目表版本、核验日期、来源和适用条件，分别维护 API 与 Codex credits 规则。
- [x] 明确“按当前价格重估”的展示口径；保留版本信息供变化追踪，若支持历史口径则另行定义生效区间。
- [x] 提供未知模型/模式明细，包括原始名称、token 数量和占比。
- [x] 支持显式人工价格或别名映射，校验配置并标记自定义来源；继续禁止仅凭名称前缀继承价格。
- [x] 保留现有价格签名和受影响日志重算机制，补充价格版本变化说明。
- [x] 展示当前价目表版本与核验日期，便于判断是否需要更新。

**验收：** 价格和别名变更可追溯；未知模型不会自动套用相近型号；更新后重算一致且不丢失有效周观察基线；API 等价、credits 等价和官方余额保持清楚区分。

相关代码：[PricingCatalog.swift](Sources/CodexRateLimitsCore/PricingCatalog.swift)、[pricing.json](Sources/CodexRateLimitsCore/Resources/pricing.json)、[TokenCostEstimator.swift](Sources/CodexRateLimitsCore/TokenCostEstimator.swift)、[CodexCreditEstimator.swift](Sources/CodexRateLimitsCore/CodexCreditEstimator.swift)。使用说明：[价格配置](docs/pricing.md)。

## TODO-06：展示数据时效，完善刷新恢复

**完成记录（2026-09-12）：** 为额度、官方余额、重置券和本机统计建立独立刷新状态、成功时间及数据时间。界面区分刷新中、部分成功、失败、字段缺失与过期；失败保留同账户旧数据并弱化显示，过期时暂停估值/预测，其他来源的刷新不再清除错误。接入菜单打开期间的定时更新、睡眠唤醒和网络恢复；请求合并为每类最多一个运行任务及一个后续请求，连续失败退避最多 10 分钟，支持手动刷新和重建。使用账户身份及请求代次丢弃迟到结果；为接口、网络、扫描和缓存锁加入有时限的取消与清理，账户变更或取消不提交扫描缓存。CLI/MCP 追加时效元数据。新增 19 项测试，全部 147 项测试及隔离 CLI/MCP 集成检查通过，包括缺失字段、超时子进程清理和恢复。真实日志缓存副本验证：缓存读取约 0.12 秒、增量约 0.06 秒、重建约 40 秒，峰值内存约 62 MB；tokens、API 金额、credits 一致，账户基线及周采样历史未变。新增状态行已用 AppKit 离屏渲染检查；真实断网、睡眠和登录切换的人工步骤见使用说明，未自动操作系统网络或真实登录。

**问题：** 修复前网络失败后会保留先前的周额度和预测，但没有明确展示最后成功更新时间。

- [x] 为额度、余额、重置次数和本机统计分别记录最后成功时间及刷新状态。
- [x] 区分正在刷新、刷新成功、部分成功、失败及数据过期；过期后弱化旧数据并展示更新时间。
- [x] 避免其他刷新动作覆盖仍有效的失败提示。
- [x] 在系统唤醒、网络恢复后及时刷新，并合并重复刷新请求。
- [x] 连续失败采用有上限的重试退避，保留手动刷新入口。
- [x] 旧账户或旧刷新请求的迟到结果不能覆盖当前账户状态。

**验收：** 断网后用户能辨认旧数据；恢复连接后自动更新；睡眠唤醒和连续失败不会导致永久卡在刷新中或积压请求。

相关代码：[RefreshState.swift](Sources/CodexRateLimitsCore/RefreshState.swift)、[UsageRefreshController.swift](Sources/CodexRateLimitsCore/UsageRefreshController.swift)、[AppDelegate.swift](Sources/CodexRateLimitsBar/AppDelegate.swift)。使用说明与人工验收：[数据时效与刷新恢复](docs/refresh.md)。

## TODO-07：完善回归测试和验证流程

**完成记录（2026-09-12）：** 将常规回归、CLI/MCP 跨进程样例、AppKit 离屏检查及独立应用资源检查统一到 `make verify`；默认使用临时 Foundation home、假凭据和禁止联网的测试进程，真实账户读取独立为 `make verify-live`。新增跨午夜、fork、复制、归档移动、同大小替换、未完成记录、修复及价格迁移的增量/完整重建/重启对比，比较完整统计与周观察，并断言独立已知的 tokens/金额。AppKit 验证编译实际视图，覆盖 7 种状态并输出浅色/深色图片；启动入口单独保留在 `main.swift`。新增可重复的大日志性能入口 `make benchmark`，记录冷扫描、无变化、追加、重启、重建、内存和缓存大小。检查发现并修复了两处问题：缺失归档目录创建前后 `/private/var` 与 `/var` 的路径别名变化会重置周观察，以及统计不可用时悬停详情仍显示零 credits；旧缓存的等价路径别名保持有效基线。151 项常规测试、CLI/MCP、7 种 AppKit 场景和独立应用资源检查通过；256 MiB 合成日志的全量/增量金额一致：冷扫描约 1.41 秒、重建约 1.70 秒、无变化增量约 0.0015 秒、追加约 0.13 秒，峰值 RSS 约 31 MB、缓存约 3.9 KB；详细结果写入 `.build/verification/benchmark.json`。实际网络、睡眠和登录切换继续保留明确的人工验收步骤，不能用离屏渲染代替。

- [x] 将本次隔离样例转为仓库内可重复运行的测试，不依赖临时目录中保留的审查脚本。
- [x] 覆盖账户切换、提醒隔离、文件替换、改名副本、跨 home 来源归属、读取失败和合法 JSON 空白。（随 TODO-01～03 完成）
- [x] 调整目前通过同大小覆写来证明缓存复用的测试：正常重启应复用缓存，内容替换后应正确失效。（随 TODO-02 完成）
- [x] 补充估值稳定性、价目表迁移和刷新状态测试；每项修复同步加入相应测试。（随 TODO-04～06 完成）
- [x] 对比全量与增量扫描结果，并保留跨午夜、fork、归档移动和重启等既有回归场景。
- [x] 统计逻辑变化后验证 GUI、CLI、MCP 数据一致；涉及扫描策略时检查大日志耗时和内存。
- [x] 将适合自动运行的检查接入统一验证流程；界面时效及恢复行为保留必要的人工验收步骤。

**验收：** 关键缺陷有会在旧实现上失败、在修复后通过的测试；测试使用隔离数据，不依赖真实账户或修改用户会话。

相关目录：[Core 测试](Tests/CodexRateLimitsCoreTests/)、[集成测试](Tests/Integration/)、[AppKit 验证](Tests/UI/)、[Makefile](Makefile)。使用说明：[回归验证](docs/verification.md)。

## TODO-08：拆分 Backend 和主界面职责

**完成记录（2026-09-12）：** 先建立[分阶段实施计划](docs/refactoring-plan.md)，再依次迁移官方数据访问、本机扫描/缓存、外部入口和界面刷新控制。Backend 从 2,672 行缩为 89 行的公开读取入口；官方客户端、响应转换、进程/HTTP 传输、扫描器、缓存存储、CLI/MCP 和插件安装各自归位，复用既有认证上下文。刷新任务及唯一数据状态迁入不依赖 AppKit 的 Core 控制器，可注入客户端、历史处理、执行器和时钟；AppDelegate 缩为 465 行，菜单卡片、偏好与系统事件接入分别独立。缓存仍使用原 v4 字段及编码，四个卡片的绘制内容保持一致。新增 11 项控制器测试，全部 162 项常规测试、隔离 CLI/MCP、7 种 AppKit 场景和独立应用资源验证通过。256 MiB 合成日志冷扫描约 1.51 秒、重建约 1.51 秒、无变化增量约 0.0016 秒、追加约 0.15 秒，峰值 RSS 约 29 MB、缓存 3,870 字节，全量/增量金额一致，相比 TODO-07 无明显性能退化。备份旧应用和状态后安装，四项实际读取成功、扫描完整，账户及周观察基线不变，460 条已有样本保留。真实网络与睡眠事件仍使用既有人工验收步骤。

**现状：** 审查时 `CodexBackend.swift` 约 2,290 行，菜单栏文件约 1,681 行（TODO-07 将启动入口保留在 `main.swift`，视图及应用逻辑移至 `MenuBarApp.swift`），统计、接口和界面调度的变更影响范围较大。

- [x] 从 Backend 拆出本机扫描器及缓存存储、官方接口客户端、认证/账户上下文。
- [x] 将 MCP 服务、CLI 分发和插件安装逻辑从数据访问模块分离。
- [x] 将菜单栏刷新调度及状态管理与 AppKit 视图绘制分离。
- [x] 为接口客户端和刷新调度提供可替换依赖，便于测试失败、恢复和迟到响应。
- [x] 分阶段迁移，每一步保持 CLI/MCP 输出、缓存兼容性和已验证统计行为。

**验收：** 单独修改价格、扫描或刷新状态时，无需同时改动无关模块；既有验证通过，性能无明显退化。

相关说明：[模块边界](docs/architecture.md)、[实施计划与验收记录](docs/refactoring-plan.md)。
