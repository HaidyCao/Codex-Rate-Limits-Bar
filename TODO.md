# 项目优化 TODO

基于 2026-09-11 的项目审查整理。以下均为待完成事项；复现结果来自隔离样例，不代表真实账户已经发生相同错误。

每次选择一项推进：补充有意义的回归测试、实施修复、完成验收，再勾选对应任务并记录变更。统计修复优先于结构重构。

## 优先级与执行顺序

| 编号 | 优先级 | 任务 | 执行安排 |
| --- | --- | --- | --- |
| TODO-01 | P1 | 统一账户来源，隔离预测和提醒历史 | 第一批 |
| TODO-02 | P1 | 修复会话去重、来源归属和缓存失效 | 第一批 |
| TODO-03 | P1 | 修复静默漏扫，展示统计完整性 | 第一批 |
| TODO-04 | P2 | 改善周额度金额估值的可信度 | 依赖 TODO-01～03 |
| TODO-05 | P2 | 价目表版本化与未知模型维护 | 第二批 |
| TODO-06 | P2 | 展示数据时效，完善刷新恢复 | 第二批 |
| TODO-07 | P2 | 完善回归测试和验证流程 | 跟随各项实施 |
| TODO-08 | P3 | 拆分 Backend 和主界面职责 | 关键修复及测试完成后 |

## TODO-01：统一账户来源，隔离预测和提醒历史

**问题与证据：** 额度请求跟随 `CODEX_HOME`，重置次数查询默认读取 `~/.codex/auth.json`；预测和提醒共用 `quota-history.json`，没有账户维度。切换账户的样例中，混入旧样本后预测消耗速度由独立计算的约 0.51 个百分点/小时变成 15，并标记为高置信度；旧账户的提醒去重状态还会阻止新账户提醒。

- [ ] 建立统一账户上下文，明确账户标识、Codex home、认证来源和额度类型。
- [ ] 额度、官方 credits 余额、重置次数及本机周观察使用一致的账户来源；保留“今日全机合计”的明确口径。
- [ ] 优先使用 `account/rateLimits/read` 的 `rateLimitResetCredits`，处理字段缺失、仅返回数量和旧版本兼容；需要回退时仍使用同一账户认证。
- [ ] 按账户、目录和额度类型隔离预测样本、提醒状态及周金额观察；识别同一目录内重新登录其他账户的情况。
- [ ] 限定重置时间漂移的历史迁移只能发生在同一账户上下文内。
- [ ] 制定旧缓存迁移规则；无法确认归属的历史不直接并入新账户，同时保留可以确认归属的有效观察基线。
- [ ] 在界面或详情中展示数据所属账户/来源，避免混淆全机统计与当前账户额度。

**验收：** 两个 home、同一 home 切换账户、不同额度类型互不串样本和提醒；同账户正常时间漂移仍能保留历史；重置次数和额度始终来自同一账户。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift)、[QuotaForecast.swift](Sources/CodexRateLimitsCore/QuotaForecast.swift)。接口依据：[官方 App Server 文档](https://learn.chatgpt.com/docs/app-server#6-rate-limits-chatgpt)。

## TODO-02：修复会话去重、来源归属和缓存失效

**问题与证据：** 当前主要按文件名去重、按长度和游标决定增量读取。已复现同大小替换后仍返回旧值、改名副本重复计数，以及跨 home 去重后丢失当前账户的周消耗。

- [ ] 结合会话 ID、文件标识、内容指纹等信息识别会话和文件变化，避免仅依赖文件名。
- [ ] 检测同大小覆写、原子替换、截断后重写和普通追加，按需重建受影响文件的统计。
- [ ] 同一会话改名或复制后只统计一次；同名但不同会话的文件不能错误合并。
- [ ] 明确同一会话存在分叉内容副本时的处理规则，不仅按文件大小选择副本。
- [ ] 去重时保留全部来源归属，确保全机合计去重不会排除当前账户应计入的周消耗。
- [ ] 保留既有 fork 导入事件去重、跨午夜基线和价格重算行为。
- [ ] 提供“重建统计”入口，明确重建范围，保留仍有效的账户及周观察基线。

**验收样例：** 同大小日志从 100 tokens 改成 900 后，增量和全量扫描均返回 900；100 tokens 的相同会话改名副本合计仍为 100；跨 home 的相同日志全机只计一次，当前账户周消耗仍为应有的 `$0.40`。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift) 中的扫描、文件状态和去重逻辑。

## TODO-03：修复静默漏扫，展示统计完整性

**问题与证据：** 一个文件无法读取时，结果仍可能是 `error=nil`、定价覆盖率 100%；合法 JSON 仅在冒号后增加空格，就会被当前预筛选跳过。缺失请求模式或上下文信息时采用默认费率，也不等于计费信息完整。

- [ ] 让 usage 事件预筛选兼容合法 JSON 空白和字段顺序，同时验证大日志扫描性能。
- [ ] 将目录遍历失败、文件读取失败、解析失败和被跳过的重要记录纳入结构化诊断。
- [ ] 分别表达扫描完整性、模型定价覆盖率、默认计费条件所占比例，避免把单一覆盖率当作整体准确率。
- [ ] 除现有模式缺失计数外，跟踪请求上下文缺失等会影响费率判断的信息。
- [ ] 明确空目录、无法访问目录、全部记录解析失败和真实零用量的区别。
- [ ] UI、CLI、MCP 使用一致的完整性状态；界面显示简短提示，详情提供失败数量和原因。
- [ ] 数据不完整时，对相关估值降低置信度或暂停推算；文件恢复可读后自动补齐统计。

**验收：** 不可读文件或格式兼容问题不会悄悄产生“完整统计”；冒号后带空格的有效日志与紧凑 JSON 统计一致；有已知价格但信息缺失的数据可与完整计费记录区分。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift)、[Models.swift](Sources/CodexRateLimitsCore/Models.swift)、[AppText.swift](Sources/CodexRateLimitsCore/AppText.swift)。

## TODO-04：改善周额度金额估值的可信度

**问题：** 当前额度变化达到 2 个百分点、token 定价覆盖率达到 95% 即给出单点金额。整数百分比、更新延迟和短样本容易放大波动；token 覆盖比例也不等于金额覆盖比例。

- [ ] 使用多个观察区间评估估值稳定性，制定最小观察时长和有效变化量规则。
- [ ] 对齐本机消耗与官方额度采样时间，处理延迟更新和不连续样本。
- [ ] 展示样本跨度、有效样本量、置信度或估值范围，避免不必要的小数精度。
- [ ] 将未知模型、默认费率假设和扫描完整性纳入可信度判断，不单靠 95% token 覆盖率放行。
- [ ] 识别账户切换、额度重置和明显异常区间；排除、降权或暂停估值，并给出原因。
- [ ] 持续标明其他设备、云端活动不在本机分子中；周金额属于本机推算的 API 等价估值。

**验收：** 短样本和阶梯式百分比更新不会产生看似精确且高置信度的金额；稳定样本逐步收敛；不可靠数据会给出明确状态。

相关代码：[CodexBackend.swift](Sources/CodexRateLimitsCore/CodexBackend.swift) 中的周金额观察与估值、[AppText.swift](Sources/CodexRateLimitsCore/AppText.swift)。

## TODO-05：价目表版本化与未知模型维护

**问题：** API 价格、Codex credits 价格及模型别名写在 Swift 中，新增模型需要改代码和发版；价格调整后，用户也需要知道已有等价金额为什么变化。

- [ ] 将价目表与模型别名整理为可校验的独立配置，保留内置默认版本。
- [ ] 记录价目表版本、核验日期、来源和适用条件，分别维护 API 与 Codex credits 规则。
- [ ] 明确“按当前价格重估”的展示口径；保留版本信息供变化追踪，若支持历史口径则另行定义生效区间。
- [ ] 提供未知模型/模式明细，包括原始名称、token 数量和占比。
- [ ] 支持显式人工价格或别名映射，校验配置并标记自定义来源；继续禁止仅凭名称前缀继承价格。
- [ ] 保留现有价格签名和受影响日志重算机制，补充价格版本变化说明。
- [ ] 展示当前价目表版本与核验日期，便于判断是否需要更新。

**验收：** 价格和别名变更可追溯；未知模型不会自动套用相近型号；更新后重算一致且不丢失有效周观察基线；API 等价、credits 等价和官方余额保持清楚区分。

相关代码：[TokenCostEstimator.swift](Sources/CodexRateLimitsCore/TokenCostEstimator.swift)、[CodexCreditEstimator.swift](Sources/CodexRateLimitsCore/CodexCreditEstimator.swift)。

## TODO-06：展示数据时效，完善刷新恢复

**问题：** 网络失败后会保留先前的周额度和预测，但没有明确展示最后成功更新时间。

- [ ] 为额度、余额、重置次数和本机统计分别记录最后成功时间及刷新状态。
- [ ] 区分正在刷新、刷新成功、部分成功、失败及数据过期；过期后弱化旧数据并展示更新时间。
- [ ] 避免其他刷新动作覆盖仍有效的失败提示。
- [ ] 在系统唤醒、网络恢复后及时刷新，并合并重复刷新请求。
- [ ] 连续失败采用有上限的重试退避，保留手动刷新入口。
- [ ] 旧账户或旧刷新请求的迟到结果不能覆盖当前账户状态。

**验收：** 断网后用户能辨认旧数据；恢复连接后自动更新；睡眠唤醒和连续失败不会导致永久卡在刷新中或积压请求。

相关代码：[main.swift](Sources/CodexRateLimitsBar/main.swift) 中的刷新调度、结果应用和失败处理。

## TODO-07：完善回归测试和验证流程

- [ ] 将本次隔离样例转为仓库内可重复运行的测试，不依赖临时目录中保留的审查脚本。
- [ ] 覆盖账户切换、提醒隔离、文件替换、改名副本、跨 home 来源归属、读取失败和合法 JSON 空白。
- [ ] 调整目前通过同大小覆写来证明缓存复用的测试：正常重启应复用缓存，内容替换后应正确失效。
- [ ] 补充估值稳定性、价目表迁移和刷新状态测试；每项修复同步加入相应测试。
- [ ] 对比全量与增量扫描结果，并保留跨午夜、fork、归档移动和重启等既有回归场景。
- [ ] 统计逻辑变化后验证 GUI、CLI、MCP 数据一致；涉及扫描策略时检查大日志耗时和内存。
- [ ] 将适合自动运行的检查接入统一验证流程；界面时效及恢复行为保留必要的人工验收步骤。

**验收：** 关键缺陷有会在旧实现上失败、在修复后通过的测试；测试使用隔离数据，不依赖真实账户或修改用户会话。

相关目录：[Core 测试](Tests/CodexRateLimitsCoreTests/)、[Makefile](Makefile)。

## TODO-08：拆分 Backend 和主界面职责

**现状：** 审查时 `CodexBackend.swift` 约 2,290 行，菜单栏 `main.swift` 约 1,681 行，统计、接口和界面调度的变更影响范围较大。

- [ ] 从 Backend 拆出本机扫描器及缓存存储、官方接口客户端、认证/账户上下文。
- [ ] 将 MCP 服务、CLI 分发和插件安装逻辑从数据访问模块分离。
- [ ] 将菜单栏刷新调度及状态管理与 AppKit 视图绘制分离。
- [ ] 为接口客户端和刷新调度提供可替换依赖，便于测试失败、恢复和迟到响应。
- [ ] 分阶段迁移，每一步保持 CLI/MCP 输出、缓存兼容性和已验证统计行为。

**验收：** 单独修改价格、扫描或刷新状态时，无需同时改动无关模块；既有验证通过，性能无明显退化。
